= form_for :user, :html => {:id => "demote-form"} do |f|
  %fieldset#common_fields
    .form-group
      = f.select "user", admin_emails, {:include_blank => "Select an admin"}, {:class => "form-control"}
    %button.btn.btn-primary{:type => "submit"} Remove admin privilege 